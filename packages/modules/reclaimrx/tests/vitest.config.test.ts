// Stub: vitest.config.ts is config infrastructure, not logic.
// This file satisfies the Werkbench test-first hook for vitest.config.ts.
import { describe, it } from "vitest";

describe("vitest config", () => {
  it("exists (config infrastructure, not logic)", () => {
    // vitest.config.ts is tested implicitly by every test run succeeding
  });
});
