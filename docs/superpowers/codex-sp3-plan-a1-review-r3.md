# SP-3 Plan A1 — Adversarial Re-Review R3
**Reviewer:** Claude Sonnet 4.6 (sub-agent adversarial review)
**Date:** 2026-05-19
**Plan:** docs/superpowers/plans/2026-05-18-sp3-plan-a1-models-migration.md (post-R2 cleanup)
**Prior verdict:** docs/superpowers/codex-sp3-plan-a1-review-r2.md (GO-WITH-CHANGES, 3 cleanup items)

---

## Verdict
**GO**

---

## Summary

All three R2 GO-WITH-CHANGES cleanup items are confirmed clean:

1. **Task 7 ALTER banner (plan:1803)** — Reads exactly `# 7. ALTER reclaimrx_investigations — add 12 SP-3 scalar columns`. Zero hedging prose about "no schema= kwarg" or "Determine whether the table is in reclaimrx schema or public schema" in the surrounding lines. The only "default schema" language remaining is in the migration header comment (plan:1401) explaining the resolution — this is correct explanatory context, not hedging on execution targets.

2. **Column count consistency** — No "11 SP-3 columns" or "11 scalar columns" or "add 11" patterns anywhere in the document. All three explicit count references say "twelve" / "12 SP-3 scalar columns": plan:17, plan:582, plan:1803.

3. **test_model_indexes.py module-scope imports (Task 4, plan:2163–2175)** — All six new model imports (`AccumulatorAnomaly`, `FraudRing`, `GraphRun`, `OutboxEvent`, `ThresholdConfig`, `ThresholdConfigAudit`) appear at module scope after `from __future__ import annotations` (plan:2163), before any test function definition. No imports inside test functions in this file.

No new regressions introduced by the R2 cleanup pass:

- **TenantScopedMixin**: Not used anywhere in executable code. Plan:44 and plan:58 explicitly document it as INCOMPATIBLE and confirm it is NOT used by A1.
- **schema=_SCHEMA / {_SCHEMA}.foo**: Zero matches in executable migration code. The `No schema= kwarg` phrase at plan:1402 is the explanatory comment confirming no schema kwarg is used.
- **payment.hold_released event name**: Consistent throughout — plan:427, plan:429, plan:966, plan:1207, plan:1215, plan:2068 all use `payment.hold_released`. Not renamed to `fwa.hold_released`.
- **Downgrade reversal order**: Correct. Upgrade creates graph_runs → fraud_rings → accumulator_anomalies → threshold_configs → threshold_config_audits → outbox_events, then ALTERs. Downgrade drops ALTERs first (payment_holds column, investigations columns), then tables in exact reverse: outbox_events → threshold_config_audits → threshold_configs → accumulator_anomalies → fraud_rings → graph_runs.

---

## Findings

### NIT-1 — Duplicate DROP CONSTRAINT in downgrade (pre-existing, not introduced by R2 cleanup)
**Location:** plan:1884–1886 and plan:1889–1891  
**Problem:** `ALTER TABLE reclaimrx_payment_holds DROP CONSTRAINT IF EXISTS ck_payment_holds_status` appears twice in sequence in `downgrade()`. The second call is a no-op (PostgreSQL `IF EXISTS` is idempotent), so this is harmless at runtime.  
**Context:** This was present before R2 and was not flagged — R2 introduced a comment block between the duplicate calls (`# R1 CONCERN 8 fix: downgrade is the exact reverse of upgrade.`), making the duplication visible. Not introduced by R2 cleanup.  
**Fix:** Delete lines 1884–1886 (the first, uncommented duplicate call). Keep the one under the explanatory comment at plan:1889–1891.  
**Execution risk:** None — `IF EXISTS` means both paths produce the same result.

### NIT-2 — Function-local imports of Investigation and PaymentHold in test_new_models_instantiate.py (pre-existing)
**Location:** plan:474, 494, 500, 506, 511 (`Investigation`); plan:547, 556, 561 (`PaymentHold`)  
**Problem:** The existing ORM models `Investigation` and `PaymentHold` are imported inside individual test methods rather than at module scope.  
**Context:** The six *new* models are correctly imported at module scope (plan:109–116) — this satisfies R1 BLOCK 5. The existing models (`Investigation`, `PaymentHold`) are already registered in `Base.metadata` by the pre-existing tables.py module-load, so lazy import does not break `create_all()`. This was not flagged by R1 or R2 and is not a new regression.  
**Fix (optional):** Move `Investigation` and `PaymentHold` imports to module scope for consistency. Not execution-blocking.

---

## Recommendation

Plan A1 is ready to execute. The two NIT findings are pre-existing and non-blocking:

- NIT-1 (duplicate DROP CONSTRAINT) is cosmetic — `IF EXISTS` makes it safe. Fix in the same commit during execution cleanup if desired.
- NIT-2 (lazy imports of existing models) does not affect test correctness or `create_all()` ordering. Fix for style consistency only.

No further review round required. Proceed to execution.

---

*Generated by Claude Sonnet 4.6 adversarial re-review (R3) — 2026-05-19*
