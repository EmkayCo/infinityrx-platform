"""Local shim for shared dependencies (db, auth, events) for reclaimrx module.

Follows the same pattern as modules/core-platform/src/_shim.
Swap these imports to shared.* when the real packages are integrated.
"""
