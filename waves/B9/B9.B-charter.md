# Wave B9.B — Tier A: 113 simple lookup tables

**Status:** OPEN (2026-05-12)
**Predecessor:** B9.A CLOSED — `a114303`
**Plan reference:** `waves/B9/plan.md:133-173`
**Estimated:** ~10 sessions (12 tables/session)
**FDW manifest target:** 66 → 179 (+113)

## Scope

113 Tier A tables from the FDB NDDF Plus drop — simple lookup
descriptors (brand/route/strength/form/manufacturer) and small
xref tables across NDDF BASICS, MEDNAMES, ETC, TALL MAN PLUS, XRF.

Tier A definition (per `recon.md` §3):
- ≤ 5 columns
- Mostly < 10K rows
- No FK joins beyond Phase 09 baseline
- Ingestible via generic `TableSpec`-driven adapter (no per-table code)

## C1 — F4 pre-opening obligation (COMPLETED in B9.A close)

The first B9.B commit that opens this wave already shipped:
`modules/drug-database/drug_database/services/fdb_specs.py` +
`modules/drug-database/tests/unit/test_fdb_contract_coverage.py`.

This locks in the registry-vs-contract coverage invariant before
any Tier A spec lands. See codex GATE-CLOSE R1 MEDIUM 2.

## C2+ — Per-session shape (12 tables each)

Each B9.B session lands:

- 12 new `TableSpec` definitions in `fdb_specs.py::REGISTERED_SPECS`
- 12 contract-test wirings (each calls
  `assert_delta_semantics_acd_cycle` + the other 5 contract helpers)
- 12 names appended to `CONTRACT_TESTED_SPECS`
- Partial loader registry update (`--mode fdb_tier_a` group)

## Single migration at session ~5 mid-point

One alembic migration `0009_fdb_tier_a.py` covering all 113 Tier A
tables. SPEC-locked granularity (`charter.md:75-79`). Generated via
`scripts/gen_fdb_tier_migration.py` (B9.A C2).

Operator dry-run requirement (ADVERSARIAL R1 A2):

```bash
source infrastructure/scripts/switch_env.sh dev
alembic upgrade head        # forward
alembic downgrade -1        # reverse
alembic upgrade head        # re-apply
```

All three must succeed; capture evidence to
`waves/B9/B9.B-dryrun_evidence.md`.

## B9.B mini-GATE-CLOSE criteria

Per `waves/B9/plan.md:162-171`:

- All 113 Tier A tables row-count-reconciled vs `RECORD_COUNTS.TXT`
  (±0.1% tolerance via `assert_row_count_reconciles`)
- `setup_fdw.sh --verify` PASS 179/179 both env DBs
- All 113 contract tests green (no parse warnings, all spec table
  names appear in CONTRACT_TESTED_SPECS)
- Sampled behavioral tests: 10 randomly-picked Tier A tables
  exercise a non-trivial query (FK join, where-clause, sort)
- Migration `0009_fdb_tier_a.py` apply + reverse + apply: clean
- Weekly delta path idempotent: re-run `load_fdb.py --mode
  fdb_tier_a` produces 0 net new rows
- `pytest --collect-only` count = baseline + 113 contract tests
- F4 contract coverage test green
- Phase 09 baseline (C12) unchanged
- Codex B9.B mini-GATE-CLOSE consult: GO

## Session estimation

10 sessions × 12 tables/session = 120 capacity vs 113 actual.
Overflow capacity covers operator review breaks between contiguous
functional groups (e.g., all NDDF BASICS lookups one session; all
ETC lookups another) per `plan.md:171`.

## Risk register inheritance

20 ADVERSARIAL attacks from R1 + R2 stay in scope. The most relevant
for B9.B specifically:

- **T1 (latin-1 decode)**: every Tier A contract runs the latin-1
  decode smoke on the first 1000 rows (`assert_latin1_decode_smoke`).
- **T12 (UPD A/C/D probe)**: every Tier A spec with
  `has_transaction_code=True` runs `assert_upd_transaction_codes`.
  Tables that fail get re-classified to TRUNCATE_RELOAD.
- **N4 (silent skip)**: every Tier A contract runs
  `assert_no_parse_warnings`; allowlist stays empty unless a
  consult-round signs off via `parse_warning_allowlist.md`.

## When B9.B closes

When all 113 Tier A tables pass the per-spec contract suite, the
mini-GATE-CLOSE criteria are green, AND codex returns GO on the
B9.B close consult. Next: B9.C — 66 NDC/GCN-keyed joins.
