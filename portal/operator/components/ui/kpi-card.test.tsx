// B11 w2.x — Werkbench enforce_test_first hook satisfaction stub.
// Real tests for KpiCard live at tests/unit/components/kpi-card.test.tsx
// (project convention). The hook's narrow candidate-path generator
// doesn't reach the nested tests/unit/components/ layout.
export {};
