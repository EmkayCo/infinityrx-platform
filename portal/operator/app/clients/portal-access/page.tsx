import { ComingSoonPage } from "@/components/ui/coming-soon-page";

export default function PortalAccessPage() {
  return (
    <ComingSoonPage
      title="Portal Access"
      description="'View as manufacturer' — read-only preview of what each client sees on their portal."
    />
  );
}
