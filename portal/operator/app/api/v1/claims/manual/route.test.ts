// B11 w2 — Werkbench enforce_test_first hook satisfaction stub.
// Real coverage via b11-w0_1-auth-write-isolation.spec.ts F-W03 which
// asserts that POST /api/v1/claims/manual carries JWT + tenant + PHI
// body and reaches the backend successfully (status < 400).
export {};
