import { ComingSoonPage } from "@/components/ui/coming-soon-page";

export default function ReportLibraryPage() {
  return (
    <ComingSoonPage
      title="Report Library"
      description="Pre-built report templates — run, preview, and schedule delivery."
    />
  );
}
